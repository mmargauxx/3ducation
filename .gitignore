# Dependencies
node_modules/

# wp-env / local WordPress
.wp-env.override.json

# OS / editor noise
.DS_Store
*.log
*.swp

# Build / temp
*.zip

# Local audit / review artifacts (not theme source)
*.pdf
placeholder-review.pdf

# Skill / agent tooling artifacts (not theme source)
.agents/
.claude/
skills-lock.json
_import/
